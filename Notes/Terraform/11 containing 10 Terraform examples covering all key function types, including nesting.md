Here is the **downloadable ZIP file** containing 10 Terraform examples covering all key function types, including nesting:

🔗 [Download Terraform Function Examples (.zip)](sandbox:/mnt/data/terraform_function_examples.zip)

These examples demonstrate:

- String functions (`join`, `replace`, `lower`)
    
- Numeric functions (`max`, `min`)
    
- Collection functions (`contains`, `lookup`, `merge`, `zipmap`)
    
- Date/time (`timestamp`, `timeadd`)
    
- Hashing (`md5`)
    
- Type conversion (`tostring`, `tonumber`)
    
- Sensitive handling (`sensitive`, `nonsensitive`)
    
- UUID generation (`uuid`)
    
- Nesting functions (`lower(join(...))`)
    

Let me know when you're ready to proceed to the next topic.